import { CoReplaceTextPipe } from './co-replace-text.pipe';

describe('CoReplaceTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CoReplaceTextPipe();
    expect(pipe).toBeTruthy();
  });
});
